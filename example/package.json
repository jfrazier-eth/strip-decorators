{
  "name": "example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf stripped & rimraf full",
    "build": "npm run clean && ts-node strip.ts && tsc -p ./tsconfig.full.json",
    "test": "npx ts-node ./test.ts"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@nestjs/swagger": "^5.2.1"
  },
  "devDependencies": {
    "ts-node": "^10.7.0"
  }
}
